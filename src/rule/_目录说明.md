## 目录说明
- **overwrite**：存放的最终生成`可直接使用`的`覆写`配置文件。

- **github**：存放GitHub站点`直连`的域名规则。

- **direct**：存放自定义`直连`的域名规则文件。

- **reject**：存放自定义`拦截`的域名规则文件。

- **ads**：用于存放自定义的去广告规则文件，其中也可能有`_src`目录中生成的规则文件。

- **_src**：用于存放自定义生成去广告规则的所有资源(包括代码，规则，模版等)，用于开发去广告规则一般不直接使用。
